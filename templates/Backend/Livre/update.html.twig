{% extends 'base.html.twig' %}

{% block title %}
    Modification du livre {{ livre.titre }} | {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container mt-4">
        <h1 class="text-center">Modification du livre {{ livre.titre }}</h1>
        {% include 'Backend/Livre/_form.html.twig' with {buttonLabel: 'Modifier'} %}
        <a href="{{ path('admin.livres.index') }}" class="btn btn-primary mt-4">Retour à la liste</a>
    </section>
{% endblock %}
